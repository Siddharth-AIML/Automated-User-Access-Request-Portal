spring:
  application:
    name: accessportal

  datasource:
    url: jdbc:postgresql://localhost:5432/access_portal
    username: postgres
    password: Shalan@1984

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081